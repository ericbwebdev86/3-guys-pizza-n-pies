const router = require('express').Router();
const sequelize = require('../config/connection');

// get all products
// get one product
// create product
// update product
// delete product

module.exports = router;